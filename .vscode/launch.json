{
    "configurations": [
        {
            "name": "launch-agent",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/agent/go-service",
            "args": [
                "{AGENT_ID}"
            ],
            "cwd": "${workspaceFolder}/install"
        }
    ]
}
